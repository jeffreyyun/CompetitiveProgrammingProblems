// Robin Hood

#include <iostream>
#include <cctype>
#include <cmath>
#include <string>
#include <algorithm>
#include <vector>
#include <climits>
using namespace std;

int main() {

	int n;
	vector<int> c;
	cin >> n >> k;

	int l = 0; r = 1e9;
	// how much could be stolen from richest
	while (l < r) {
		int g = (l+r)/2;
		if (max(a[i]-g,0) > k)
			// guess less, days not enough
		else
	}
	
	// how much could be given to poorest
	while (l < r) {
		int g = (l+r)/2;
		if (max(g-a[i], 0) > k)
			// guess less, days not enough
		else
	}

return 0;
}