#include <iostream>
#include <cctype>
#include <cmath>
#include <string>
#include <algorithm>
#include <vector>
#include <climits>
using namespace std;

int main() {

	int n,m,k;
	cin >> n >> m >> k;

	int guess;	// B(n)
	int a = 1, r = 1;


	while (() < k-1) {
		for (int a = 1; a < n; a++) {
			for (int r = 1; r < m; r++){
				// a on row b
		// (a-1)/(b) smaller than guess
			if ((a-1)/r < guess)
				sum++;
			}
			min((a-1)/2, m);
		}
		

		// sums, checks if greater or less than k-1;

		// moves guess correspondingly
	}

return 0;
}